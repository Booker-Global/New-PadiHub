import type { Plugin } from "vite";

/** No-op Vite plugin stub — original dev-tools package not present in this checkout. */
export function devToolsPlugin(): Plugin {
  return { name: "dev-tools-stub" };
}
