import type { Plugin } from "vite";

/** No-op Vite plugin stub — original FullStory plugin not present in this checkout. */
export function fullStoryPlugin(): Plugin {
  return { name: "fullstory-stub" };
}
