/** No-op Babel plugin stub — original source-mapper package not present in this checkout. */
export default function sourceMapperPlugin() {
  return {
    name: "source-mapper-stub",
    visitor: {},
  };
}
