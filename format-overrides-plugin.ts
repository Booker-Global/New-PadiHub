import type { Plugin } from "vite";

/** No-op Vite plugin stub — original format overrides plugin not present in this checkout. */
export function formatOverridesPlugin(_rootDir: string): Plugin {
  return { name: "format-overrides-stub" };
}
